---
name: sharpen
description: >
  방망이깎기 — 초기 아이디어나 기획 초안을 소크라테스 문답으로 다듬어 제품화 가능한 명세서로 만든다.
  텍스트 붙여넣기 또는 Notion URL을 입력받는다.
  Notion URL이 있으면 해당 페이지 하위에 결과물 저장, 없으면 현재 작업 디렉토리에 .md 파일로 저장.
  "아이디어 정리", "기획 구체화", "sharpen", "방망이깎기", "빠진거 채워줘", "명세서 만들어줘" 등을 언급하면 이 스킬을 사용한다.
argument-hint: "{Notion URL 또는 아이디어 텍스트}"
allowed-tools:
  - mcp__notion__notion-fetch
  - mcp__notion__notion-create-pages
  - Write
  - Agent
---

# /sharpen — 방망이깎기

🪵 방망이를 깎아봅시다

초기 아이디어의 빈 곳을 질문으로 채워, 제품 개발에 바로 착수할 수 있는 수준의 명세서로 만든다.

---

## 실행 흐름

### 0. 입력 받기

- **Notion URL** (`notion.so/...` 포함): `mcp__notion__notion-fetch`로 내용 로드
- **텍스트**: 그대로 사용
- **둘 다 없음**: "정리할 아이디어나 기획 내용을 붙여넣어 주세요."

입력을 읽고 AS-IS / TO-BE 구조가 있으면 추출, 없으면 전체를 '현재 아이디어'로 취급한다.
Notion URL 여부를 기억해둔다 — 저장 방식 결정에 사용.

### 1. 첫 번째 라운드 시작

```text
🪵 방망이를 깎아봅시다

"{제목 또는 첫 줄 요약}"

현재 상황: {핵심 1-2줄 — 없으면 "아직 구체화되지 않음"}
원하는 결과: {핵심 1-2줄 — 없으면 "아직 구체화되지 않음"}

빠진 부분을 채워나가겠습니다.

---

Round 1

{질문 1-2개}
```

### 2. 소크라테스 질문 전략

라운드당 **최대 2개**. 가장 모호한 축부터 공략한다.

| 축 (배점) | 이럴 때 묻는다 | 질문 예시 |
|----------|-------------|----------|
| Who (15) | 누가 쓰는지 불분명 | "이걸 실제로 매일 쓰게 되는 사람은 누구인가요?" |
| Why (20) | 왜 해야 하는지 근거 부족 | "이걸 안 하면 구체적으로 어떤 문제가 계속되나요?" |
| What (20) | 결과가 안 보임 | "이게 완성되면 지금과 구체적으로 뭐가 달라지나요?" |
| Scope (15) | 경계가 모호 | "이건 포함인가요, 이번엔 빠지는 건가요?" |
| Measure (15) | 성공 기준 없음 | "이게 잘 됐다는 걸 어떻게 확인할 수 있나요?" |
| Risk (15) | 리스크 미인지 | "이 작업에서 가장 걱정되는 부분이 있다면?" |

**원칙**:

- 사용자가 이미 명확히 답한 축은 건너뛴다
- 구현 방법(How)은 묻지 않는다 — 기술 스택, 아키텍처, UI는 이 단계 범위 밖
- 답을 유도하지 않는다 ("이건 X인가요?" 금지)

### 3. Clarity Judge

매 라운드 후 Agent로 명확성 판정:

```text
You are judging how clear a product planning intention is. Score 0-100.
Do NOT evaluate implementation details — only clarity of intent.

## Scoring Rubric
| Item    | Max | Criteria                       |
|---------|-----|--------------------------------|
| Who     | 15  | 사용자가 특정됨                |
| Why     | 20  | 근거 있는 이유                 |
| What    | 20  | 구체적인 결과물/변화           |
| Scope   | 15  | 포함/제외 구분됨               |
| Measure | 15  | 검증 가능한 기준               |
| Risk    | 15  | 주요 리스크 인지               |

## Input
{원본 아이디어 + 모든 라운드 Q&A}

## Output (JSON only)
{"score":75,"breakdown":{"who":{"score":12,"max":15,"note":"..."},...},"weakest":"measure","suggestion":"..."}
```

- **80점 이상** → 완료 단계로
- **80점 미만** → 점수 보여주고 다음 라운드
- **최대 10라운드** → 이후엔 현재 상태로 완료, 약한 축 명시

### 4. 점수 공유 & 다음 라운드

```text
Round {N} 결과 — 명확성 {score}/100

Who      ▓▓▓▓▓▓▓▓░░░░  {score}/15
Why      ▓▓▓▓▓▓▓▓▓▓░░  {score}/20
What     ▓▓▓▓▓▓░░░░░░  {score}/20
Scope    ▓▓▓▓▓▓▓▓░░░░  {score}/15
Measure  ▓▓░░░░░░░░░░  {score}/15
Risk     ▓▓▓▓▓▓▓▓▓░░░  {score}/15

보강 필요: {weakest축 한국어명}
→ {suggestion}

---

Round {N+1}

{질문}
```

### 5. 완료 — 명세서 생성 & 저장

**명세서 내용**:

```markdown
# {제목} — 명세서

**명확성**: {score}/100 ({rounds}라운드)
**작성일**: {오늘}

---

## 한 줄 요약
{의도를 한 문장으로}

## 대상 (Who)
{누구를 위한 것인지}

## 배경 (Why)
{왜 해야 하는지 — 안 하면 어떤 문제가 지속되는지}

## 결과물 (What)
{완성되면 눈에 보이는 구체적인 변화}

## 범위 (Scope)
**포함**: {포함 항목}
**제외**: {제외 항목}

## 성공 기준 (Measure)
{검증 가능한 기준}

## 리스크 (Risk)
{주요 리스크와 대응 방안}

---

## 원본 아이디어
{입력 원문}
```

**저장**:

- Notion URL로 시작했으면 → `mcp__notion__notion-create-pages`로 해당 페이지 하위 페이지 생성
- 텍스트로 시작했으면 → `Write`로 현재 작업 디렉토리에 `./{제목}-sharpened-spec.md` 저장

**완료 안내**:

```text
명세서가 완성되었습니다.
📄 저장 위치: {Notion 링크 또는 로컬 파일 경로}

---

다음 단계가 필요하다면:
➡️  /productify — 이 명세서를 제품화 전략으로 발전시키기
    (명세서 경로 또는 Notion URL을 함께 넘겨주세요)
```

---

## 엣지 케이스

- **입력이 너무 짧음** (한 줄): "조금 더 알려주세요 — 지금 어떤 문제가 있고, 어떻게 바뀌면 좋겠는지 자유롭게 써주시면 됩니다."
- **사용자가 "이 정도면 됐어"**: 현재 점수로 즉시 명세서 생성. 80점 미만이면 약한 축을 명세서에 '미확인 항목'으로 표시.
- **구현 제안이 들어올 때**: "구현 방법은 일단 제쳐두고, 이게 왜 필요한지부터 명확하게 만들어볼게요."
- **Notion fetch 실패**: "Notion 연결이 필요합니다. MCP 설정을 확인해주세요. 또는 내용을 직접 붙여넣어 주세요."
